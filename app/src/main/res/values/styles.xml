<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="material_btn_add" parent="Widget.AppCompat.Button.Colored">
        <item name="android:color">@color/main_dark</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="icon">@drawable/ic_add_24dp</item>
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">16dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:drawableLeft">@drawable/ic_add_24dp</item>
    </style>

</resources>